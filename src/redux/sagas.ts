import { SagaIterator } from 'redux-saga'; // @redux
//import { all, fork } from 'redux-saga/effects';
//import { saga as authSaga } from './Auth';

export function* rootSaga(): SagaIterator {
  //yield all([fork(authSaga)]);
}
