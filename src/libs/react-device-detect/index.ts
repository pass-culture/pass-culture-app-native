// True or false, we don't really care. Those constants are used either in .web files
// or within a Platform.OS === "web" condition.
const isMobile = true
const isTablet = true
const isDesktop = true

export { isDesktop, isMobile, isTablet }
