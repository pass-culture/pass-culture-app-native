import React from 'react'

import { WebShareModalProps } from './types'

export const WebShareModal = ({
  visible,
  headerTitle,
  shareContent,
  dismissModal,
}: WebShareModalProps) => {
  return null
}
