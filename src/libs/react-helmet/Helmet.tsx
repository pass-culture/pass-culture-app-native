import { PropsWithChildren } from 'react'
import { HelmetProps } from 'react-helmet'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const Helmet = (props: PropsWithChildren<HelmetProps>) => null
