import React from 'react'

import { PageWithHeader } from 'features/identityCheck/components/layout/PageWithHeader'

export const ChangeStatus = () => {
  // Don't forget to add accessibilityRole={AccessibilityRole.RADIOGROUP} accessibilityLabelledBy={titleID} on the view around the "Controller"

  return <PageWithHeader title="Profil - Changer Status" />
}
