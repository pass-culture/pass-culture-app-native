describe('AppButton Component', () => {
  it('should have 70% opacity when being pressed', () => {
    fail()
  })
  it('should have 70% opacity when being pressed', () => {
    fail()
  })
  it('should display the passculture icon when isloading is set', () => {
    fail()
  })
  describe('Disabled', () => {
    it('should have a primary-disabled background when disabled is set', () => {
      fail()
    })
    it('should not be clickable when disabled is set', () => {
      fail()
    })
  })
})
