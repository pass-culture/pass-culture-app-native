import { GestureResponderEvent } from 'react-native'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const useEscapeKeyAction = (cb?: (e: GestureResponderEvent) => void) => null
