import { GestureResponderEvent } from 'react-native'

/**
 * This component is Web only
 * @param callback
 */
export const useSpaceBarAction = (_callback?: (e: GestureResponderEvent) => void) => undefined
