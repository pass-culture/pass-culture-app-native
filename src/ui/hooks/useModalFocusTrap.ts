import { RefObject } from 'react'
import { View } from 'react-native'

/**
 * This hook is Web only
 */
export const useModalFocusTrap = (_modalRef: RefObject<View | null>, _isReady: boolean) => undefined
