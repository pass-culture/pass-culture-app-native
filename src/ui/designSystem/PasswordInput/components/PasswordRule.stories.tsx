/**
 ⚠️ Design System — Protected file
 Do not edit this file without prior approval from the Design System team.
 */

import type { Meta } from '@storybook/react-vite'
import React from 'react'

import { PasswordRuleStatus } from 'ui/designSystem/PasswordInput/enums'
import { VariantsTemplate, type Variants, type VariantsStory } from 'ui/storybook/VariantsTemplate'

import { PasswordRule } from './PasswordRule'

const meta: Meta<typeof PasswordRule> = {
  title: 'design system/inputs/PasswordInput/PasswordRule',
  component: PasswordRule,
}
export default meta

const baseProps = { title: 'Mot2Passe' }

const variantConfig: Variants<typeof PasswordRule> = [
  { label: 'PasswordRule default', props: { ...baseProps, status: PasswordRuleStatus.Default } },
  { label: 'PasswordRule valid', props: { ...baseProps, status: PasswordRuleStatus.Valid } },
  { label: 'PasswordRule invalid', props: { ...baseProps, status: PasswordRuleStatus.Invalid } },
]

export const Template: VariantsStory<typeof PasswordRule> = {
  name: 'PasswordRule',
  render: (props) => (
    <VariantsTemplate
      variants={variantConfig}
      Component={PasswordRule}
      defaultProps={{ ...props }}
    />
  ),
}
