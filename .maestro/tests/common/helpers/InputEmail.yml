appId: ${MAESTRO_APP_ID}
---
- runFlow:
    when:
      platform: iOS
    commands:
    - inputText: ${MAESTRO_VALID_IOS_EMAIL}

- runFlow:
    when:
      platform: Android
    commands:
    - inputText: ${MAESTRO_VALID_ANDROID_EMAIL}