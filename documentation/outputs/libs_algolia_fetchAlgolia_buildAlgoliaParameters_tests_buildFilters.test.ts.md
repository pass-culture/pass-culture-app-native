buildFilters
 without filter
- should return an object if no parameters are passed


 excludedObjectIds
- should return filters that excludes objectID when excludedObjectIds is not empty
- should return filters that excludes objectID when excludedObjectIds is empty


 with filter


 buildFilters

