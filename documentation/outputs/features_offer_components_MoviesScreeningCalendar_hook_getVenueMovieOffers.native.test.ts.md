getVenueMovieOffers
 useVenueMovieOffers
- should sort offers by descending last30DaysBooking order
- should return movie offers of the selected date
- should return next movie offers of a given date
- should return empty array when there are no movies on a given day or after
- should not return movies with screenings after 15 days when other movies have screenings within 15 days
- should only return movie with screenings after 15 days when other movies don't have screenings within 15 days

