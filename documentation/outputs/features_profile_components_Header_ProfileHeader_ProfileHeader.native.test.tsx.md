ProfileHeader
 ProfileHeader
- should not display subtitle with passForAll enabled
- should display the LoggedOutHeader if no user
- should display the BeneficiaryHeader if user is beneficiary
- should display the BeneficiaryHeader if user is underage beneficiary
- should display the ExBeneficiary Header if credit is expired
- should display the NonBeneficiaryHeader Header if user is not beneficiary
- should display the NonBeneficiaryHeader Header if user is eligible exunderage beneficiary

