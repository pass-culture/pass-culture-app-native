getFilterDescription
 getFilterDescription
- should return "Tout" when all types in the group are selected
- should return a comma-separated list of labels for selected types
- should return an empty string when no types are selected

