NonBeneficiaryHeader
 <NonBeneficiaryHeader/>
- should render the activation banner when user is eligible and api call returns activation banner
- should render the transition 17 to 18 banner when beneficiary's user is now 18
- should display identity check message if user identity check is pending
- should render the subscription message if there is one
- should render the younger badge for user whose eligibilty hasn’t started yet (under 15 years old)
- should not display banner or badge when user is beneficiary

