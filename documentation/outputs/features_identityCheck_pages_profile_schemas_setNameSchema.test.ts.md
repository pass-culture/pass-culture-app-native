setNameSchema
 setNameSchema
- should reject when no name
- should reject when first name has a special character
- should reject when last name has a special character
- should resolve when last name and first name are valid

