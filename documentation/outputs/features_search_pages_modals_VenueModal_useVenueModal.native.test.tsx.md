useVenueModal
 In initial state
- when start it should return falsy state
- when provide a query it should change state for the UI
- when select a venue it should change state for the UI
- when select a venue and reset it should reset the UI
- when select a venue and validate it should call the search hook and modal dismiss
- when select a venue and validate it should apply search to the context
- when nothing is to be search then search cannot be done
- should trigger logEvent "logUserSetVenue" when doApplySearch
- should reset search when pressing reset button


 useVenueModal

