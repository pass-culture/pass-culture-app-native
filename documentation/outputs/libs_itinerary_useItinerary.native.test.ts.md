useItinerary
 useItinerary
- navigates with the right app if there is only one available app
- displays a modal with all the available apps if their count is geater than 1
- should add a cancel button for iOS devices
- should open google maps web if no app is available
- should navigate with google maps web if the lib failed and only one application was available
- should display an information snackbar if the lib failed and to open the chosen navigation app

