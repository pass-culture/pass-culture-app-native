MovieOfferTile
 with screening on selected date
- should render offer component


 without screening on selected date
- should not render offer component


 with next screening date
- should render next screening button
- should open booking modal on press when nextScreeningDate is at least 15 days after today
- should trigger calendar scroll on press when nextScreeningDate is within the next 15 days


 MovieOfferTile

