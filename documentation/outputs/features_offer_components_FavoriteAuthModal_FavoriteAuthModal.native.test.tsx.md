FavoriteAuthModal
 sign in
- should navigate to sign in page with offerId in params
- should send `logSignInFromOffer` event


 sign up
- should navigate to signup page with offerId in params
- should send `logSignUpFromOffer` and `logSignUpClicked` events


 FavoriteAuthModal
- should match previous snapshot
- should send `logQuitFavoriteModalForSignIn` when closing modal

