customFocusOutline.web
 customWebFocusOutline
- should display the custom outline if isFocus = true
- should change the custom outline color if useFocus = true and uses custom color
- should change the custom outline width if useFocus = true and uses custom width
- should display the custom outline when isFocus = false
- should change the custom outline color when isFocus = false and uses custom color
- should change the custom outline width when isFocus = false and uses custom width

