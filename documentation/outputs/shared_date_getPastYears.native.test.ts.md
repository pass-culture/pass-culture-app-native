getPastYears
 getPastYears
- should return an array of years as strings in descending order
- should return an empty array if startYear is greater than currentYear
- should handle startYear as a negative value

