useThematicSubscription
 when the user is already subscribed to the page
- should consider subscribe button active
- should unsubscribe when the user clicks the subscribe button


 when the user isn't subscribed to the current page
- should consider subscribe button inactive
- should subscribe when the user clicks the subscribe button


 when the user has notifications on
- should give us the information that at least one notification is active


 when the user is already subscribed to the page
- should consider subscribe button inactive
- should not change subscribed themes when the user clicks subscribe button


 when the user isn't subscribed to the current page
- should consider subscribe button inactive
- should subscribe when the user clicks the subscribe button


 when we don't have the user
- should default to false for marketingEmail and marketingPush


 when the user has notifications off
- should give us the information that notifications are inactive


 API calls
- should show a snackbar when settings update fails
- should call onSuccess when subscription update succeeds


 Analytics
- should log when the user subscribes
- should log when the user unsubscribes


 useThematicSubscription

