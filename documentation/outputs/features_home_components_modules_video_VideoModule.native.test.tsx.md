VideoModule
 VideoModule
- should render properly
- should show modal when pressing video thumbnail
- should log ModuleDisplayedOnHomePage event when seeing the module
- should not log ModuleDisplayedOnHomePage event when module is not rendered
- should render multi offer component if multiples offers
- should render one offer component when one offer
- should render mobile design if mobile viewport
- should render desktop design if desktop viewport
- should show SeeMore button when is multiples offers and more than three offers
- should not show SeeMore button when is multiples offers and less than three offers

