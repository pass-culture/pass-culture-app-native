SetBirthday
 banner
- should display the general public message when userAge is  NonEligible.OVER_18
- should display the eligible message when userAge is set in number
- should display the eligible message when userAge is UNDER_15
- should display the eligible message when userAge is UNDER_17
- should display the eligible message when userAge is null


 <SetBirthday />
- should render correctly
- should render correctly when account creation token and email are in route params
- should call goToNextStep() when the date is selected and press the button "Continuer"
- should set a default birthdate if the user has already added his birthdate

