offerToHeadlineOfferData
 offerToHeadlineOfferData
- should transform headline offer data correctly
- should return null if offer is undefined
- should return an empty string if imageUrl is undefined
- should return undefined if geolocation is missing

