formatDistanceDate
 formatDistanceDate
- should return distance and date separated by • when both of its defined and width >= min width displayed
- should return only date when distance and date defined and width < min width displayed
- should return only distance when defined and date undefined and width >= min width displayed
- should return undefined when distance defined and date undefined and width < min width displayed
- should return only date when defined and distance undefined
- should return undefined when distance and date undefined
- should return only distance when defined and date is an empty string and width >= min width displayed
- should an empty string when only distance defined and date is an empty string and width < min width displayed
- should return only date when defined and distance is an empty string
- should return an empty string when distance and date are empty string

