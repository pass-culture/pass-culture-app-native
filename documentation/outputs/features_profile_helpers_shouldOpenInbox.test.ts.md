shouldOpenInbox
 shouldOpenInbox
- should return true if url is appUrl and contains 'openInbox' string
- should return false if url is appUrl and does not contain openInbox
- should return false if url is not appUrl and contains openInbox

