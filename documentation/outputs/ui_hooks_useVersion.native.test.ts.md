useVersion
 useVersion
- should return only the version when there are not CodePush information
- should return version and CodePush label when there are CodePush information
- should capture a Sentry issue when there is an error

