hasUserChangedParameters
 hasUserChangedParameters
- should return false if nothing is different between user and state
- should return true when user changes email opt-in
- should return true when user subscribes to a theme and unsusbcribes from another
- should return false when user has no subscribedTheme

