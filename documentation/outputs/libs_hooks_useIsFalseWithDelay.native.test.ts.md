useIsFalseWithDelay
 useIsFalseWithDelay()
- should always return true if condition is true and doesn't change
- should always return false if condition is false and doesn't change
- should always return false after delay if condition is true at first then false

