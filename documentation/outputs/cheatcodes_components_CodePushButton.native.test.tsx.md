CodePushButton
 CodePushButton
- gets the metadata on mount
- gets the partial metadata when CodePush update metdata with partial information
- gets the partial metadata when CodePush update metdata with null information
- prints that a new version is available if version mismatches

