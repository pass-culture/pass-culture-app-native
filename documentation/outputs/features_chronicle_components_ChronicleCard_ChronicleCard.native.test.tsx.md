ChronicleCard
 ChronicleCard (Mobile)
- should render the ChronicleCard component with correct title
- should display the "Voir plus" button if content overflows
- should not display the "Voir plus" button if not content overflows
- should call onSeeMoreButtonPress when "Voir plus" is clicked

