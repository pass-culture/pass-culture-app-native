SnackBar
 Basic behavior
- should forward properly text and colors
- should not display progress bar if timeout is not provided
- should render the content container when visible=true
- should render the content container when visible=false only if already rendered
- should render the illustration icon when provided when visible
- should not render the illustration icon when not provided
- should trigger onClose when the closeIcon is clicked


 Initialisation


 Visibility lifecycle
- should display the snackbar container when shown
- should hide the snackbar container when hidden
- should reset progressBar animation when visual properties changed
- should reset the timer when "refresher" is updated


 SnackBar Component

