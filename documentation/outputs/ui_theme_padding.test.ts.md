padding
 padding
- only top should return all four sides the same
- only top and right should define vertical and horizontal
- only top, right and bottom should define top, horizontal and bottom
- top, right bottom and left should define all sides

