SuggestedVenues
 <SuggestedVenues/>
- should call setSelectedVenue when selecting a venue
- should show empty component only when query is not empty and the results are not loading
- should not show empty component if the query is empty and the results are not loading
- should not show empty component if the results are still loading

