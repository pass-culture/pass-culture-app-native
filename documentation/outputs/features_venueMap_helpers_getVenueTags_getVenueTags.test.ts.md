getVenueTags
 getVenueTags function
- should return an array of venue tags when distance and venue type are provided
- should return an array of venue tags only with venue type when distance is undefined
- should return an array of venue tags only with distance when venue type is undefined
- should return an empty array when both distance and venue type are undefined

