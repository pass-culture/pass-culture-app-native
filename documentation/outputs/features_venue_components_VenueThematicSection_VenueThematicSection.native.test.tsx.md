VenueThematicSection
 when the user presses SubscribeButton
- should notify when subscription succeeds
- should notify when subscription fails
- should show logged out modal when user is not logged in
- should show notifications modal when user has no notifications activated
- should show unsubscribing confirmation modal when user subscribed and unsubscribe


 <VenueThematicSection/>
- should render null if venue has no thematic
- should render null if user is not eligible
- should render cinema block when venue is a movie theater
- should log when user login from logged out modal

