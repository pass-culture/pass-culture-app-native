VenueMapFiltersList
 VenueMapFiltersList
- should render filters list correctly
- should navigate to venue map type filter when pressing a row with filter group information
- should trigger goBack when pressing modal close button
- should return "Tout" in each filters when venue filter is empty
- should return venue types in filter descriptions

