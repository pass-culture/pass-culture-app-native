loginSchema
 loginSchema
- validate a form with all required values
- should invalidate due to an invalid email
- should invalidate due to an empty email
- should invalidate due to an empty password

