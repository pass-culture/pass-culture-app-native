BookingInformations
 formatDateTimezone()


 <BookingInformations />
- should return empty component when no offer
- should return empty component when no stock
- should return empty component when no quantity
- should render event date section when event
- should display `Gratuit` if price is 0
- should display unique price when quantity is unique
- should display stock label when the offer has it
- shouldn't display stock label when the offer hasn't any
- should display stock attributes when the offer has it
- shouldn't display stock attributes when the offer hasn't any
- should display expirationDate section when offer is digital and has expirationDate
- shouldn't display expirationDate section when offer is digital and has no expirationDate
- should display only date if it has no duration information

