OfferVenueBlock
 <OfferVenueBlock />
- should display title
- should render venue name
- should render address
- should render distance when user chose geolocation
- should not render distance when user chose 'France entière'
- should render placeholder instead of thumbnail when venue doesn't have any image
- should not render placeholder instead of thumbnail when venue has an image
- should render 'Changer le lieu de retrait' button when onChangeVenuePress is defined
- should render 'Changer de cinéma' button when venue is a cinema and onChangeVenuePress is defined
- should handle 'Changer le lieu de retrait' button press
- should not render 'Changer le lieu de retrait' button when onChangeVenuePress is undefined
- should render copy address button
- should not render copy address button when offer is cinema
- should handle copy address button press
- should render see itinerary button
- should handle see itinerary button press
- should not render see itinerary button when onSeeItineraryPress is undefined
- should not render see itinerary button when offer is cinema
- should display right icon when venue is permanent
- should not display right icon when venue is permanent but offer address is different than venue address
- should handle see venue button press
- should not render see venue button when onSeeVenuePress is undefined
- should redirect to Google Maps itinerary when pressing "Voir l’itinéraire" button
- should display offer address label when offer address is different of venue offer address
- should not display venue name when offer address is different of venue offer address and offer address has not label
- should display placeholder when offer address is different of venue offer address even if venue has an image

