VerticalVideoPlayer
 the video is finished
- should display `replay button`
- should not display `next video button` when only one video source is provided
- should display `replay button` and `next video button` when multiple video sources are provided


 VerticalVideoPlayer
- should render error view when youtube player has an error
- should not render error view without error
- should display `play video` when video has not started
- should display `pause button` and `sound button` when video is playing
- should display `keep watching` when video is paused

