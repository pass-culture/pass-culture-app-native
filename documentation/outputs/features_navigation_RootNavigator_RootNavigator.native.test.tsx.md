RootNavigator
 <RootNavigator />
- should NOT display PrivacyPolicy if splash screen is not yet hidden
- should display PrivacyPolicy if splash screen is hidden
- should not display quick access button in native
- should increment logged in session count when user is logged in
- should not increment logged in session count when user is not logged in

