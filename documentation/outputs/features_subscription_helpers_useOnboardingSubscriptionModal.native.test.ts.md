useOnboardingSubscriptionModal
 useOnboardingSubscriptionModal
- should not display modal if user is not logged in
- should not display modal if user is not eligible
- should display modal if user is eligible and in their third session
- should not display modal if user is eligible and not in their third session
- should not display modal if user has already seen it

