ImagesCarousel
 <ImagesCarousel />
- should render the carousel with the correct number of images
- should handle goBack when pressing back button
- should display the correct index in the header
- should not render dots when there is only one image

