getOfferTags
 getOfferTags
- should return the offer subcategory by default
- should return music type in tags list when offer has it
- should return music type and subtype in tags list when offer has it
- should return show type in tags list when offer has it
- should return show type and subtype in tags list when offer has it
- should not return data when it is null or undefined
- should return cinema genres in tags list when offer has it
- should handle empty cinema genres
- should handle undefined cinema genres
- should handle null cinema genres

