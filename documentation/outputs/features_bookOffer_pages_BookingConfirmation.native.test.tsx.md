BookingConfirmation
 InAppReview
- should call InAppReview Modal after 3 seconds if isAvailable and rules are respected
- should not call InAppReview Modal if isAvailable is false
- should not call InAppReview Modal if isAvailable is true and rules are not respected


 buttons
- should call display share when press share button
- should log analytics when press share button
- should go to Bookings when click on CTA
- should log analytic logSeeMyBooking when click on CTA
- should log analytic logViewedBookingPage when click on CTA


 <BookingConfirmation />
- should render correctly
- should display correct amount left text
- should not display correct amount left text when free user status

