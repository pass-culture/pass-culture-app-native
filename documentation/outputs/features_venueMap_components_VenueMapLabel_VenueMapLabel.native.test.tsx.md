VenueMapLabel
 <VenueMapLabel />
- should render the label with the correct text
- should apply the correct layout width when onLayout is triggered
- should correctly center the label based on its width

