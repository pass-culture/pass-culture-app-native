SearchHistoryItem
 SearchHistoryItem
- should not display "dans" a category or a native category when item has not it
- should display "dans" a category when item has it and not has a native category
- should display "dans" a native category when item has it and not has a category
- should display "dans" a native category when item has it and has a category too
- should execute onPress when pressing item
- should not use highlighting when query history is an empty string
- should use highlighting when query history is not an empty string

