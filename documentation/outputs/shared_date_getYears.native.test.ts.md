getYears
 getYears
- should return an array of years as strings
- should return an empty array if numberOfYears is zero
- should not handle negative values of numberOfYears
- should handle startYear as a negative value

