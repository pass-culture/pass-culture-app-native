VenueMapTypeFilter
 VenueMapTypeFilter
- should navigate to venue map when pressing modal close button
- should trigger goBack when pressing modal back button
- should display venue type filter title
- should display checkboxes associated to venue type filter
- should toggle show all checkbox when complete venue type group selected
- should toggle venue type checkbox when it is included in venue filters
- should reset venue types filters when pressing show all checkbox (checked) and venue type filter is empty without the current venue type group
- should add venue types filters of the current group when pressing show all checkbox (not checked) and venue type filter is not empty without the current venue type group
- should remove venue type filter when pressing venue type checkbox (checked)
- should add venue type filter when pressing venue type checkbox (not checked)
- should complete selection when some checkboxes are checked and pressing select all

