mappings
 useCategoryIdMapping
- should match category id mapping
- should return empty object if no data
- should return empty object if no subcategories


 useSubcategoriesMapping
- should match subcategories mapping
- should return empty object if no data
- should return empty object if no subcategories


 useSubcategoryOfferLabelMapping
- should match sub category label mapping
- should return empty object if no data
- should return empty object if no subcategories


 useSearchGroupLabelMapping
- should match search group label mapping
- should return empty object if no data
- should return empty object if no searchGroups


 useCategoryHomeLabelMapping
- should match category Home label mapping
- should return empty object if no data
- should return empty object if no subcategories


 useGenreTypeMapping
- should match category Home label mapping
- should return empty object if no data
- should return empty object if no genreTypes

