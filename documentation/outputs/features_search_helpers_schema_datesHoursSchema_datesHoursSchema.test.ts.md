datesHoursSchema
 hourSchema


 hoursSchema
- hoursSchema should fail when we have 1 value
- hoursSchema should fail when we have 3 value


 should fail
- when hasSelectedChoice is true and selectedDateChoice defined without selectedDate
- when hasSelectedChoice is true and selectedDate defined without selectedDateChoice
- when hasSelectedHours is true without selectedHours


 should validate
- without any value
- when hasSelectedDate is true with selectedDateChoice and selectedDate
- when hasSelectedHours is true with selectedHours
- when hasSelectedDate is true with selectedDateChoice and selectedDate and hasSelectedHours is true with selectedHours


 datesHoursSchema

