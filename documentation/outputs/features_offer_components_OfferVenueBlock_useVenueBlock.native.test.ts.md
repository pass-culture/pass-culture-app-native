useVenueBlock
 useVenueBlock without offer address
- should return venue name
- should return address
- should return isOfferAddressDifferent to false if offer address is not present
- should copy address to clipboard
- should show error snackbar when address is not copied


 useVenueBlock with offer address
- should return venue name from offer address
- should return address from  offer address
- should return isOfferAddressDifferent to true if offer and venue address are different
- should return isOfferAddressDifferent to false if offer and venue address are the same
- should copy address to clipboard

