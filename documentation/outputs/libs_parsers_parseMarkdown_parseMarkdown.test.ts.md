parseMarkdown
 parseMarkdown
- should parse bold text
- should parse italic text
- should parse several styles in a single string
- should ignore styles not closed correctly
- should display plain text without styles
- should parse several same styles in a single string
- should parse nested text with different styles
- should parse special characters correctly
- should not transform url pattern
- should return an empty array when style text is empty
- should return an empty array when style text has an empty style text

