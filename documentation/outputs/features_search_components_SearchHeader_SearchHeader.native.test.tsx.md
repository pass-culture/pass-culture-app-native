SearchHeader
 SearchHeader component
- should render SearchHeader
- should show LocationWidget when isDesktopViewport is false and SearchView is Landing
- should not show LocationWidget when isDesktopViewport is false and searchView is not landing
- should show SearchLocationWidget when isDesktopViewport is false
- should not show LocationWidget when isDesktopViewport is true
- should show SearchLocationWidget when isDesktopViewport is true

