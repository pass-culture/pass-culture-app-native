TicketSwiperControls
 <TicketSwiperControls/>
- should hide prev button if currentStep === 1
- should show prev button if currentStep > 1
- should hide next button if currentStep === numberOfSteps
- should show prev button if currentStep !== 1

