useDaysSelector
 useDaysSelector
- should initialize with the first date
- should update selectedDate when setSelectedDate is called with a new date
- should not update selectedDate when setSelectedDate is called with the same date
- should set selectedDate to start of day

