formatDuration
 formatDuration
- should format duration with a number of hours exactly
- should format duration with only minutes which represent less than an hour
- should format duration with one hour exactly
- should format duration with hours and minutes
- should format duration with hours and add a 0 before minutes when minutes are below 10
- should format duration with zero duration

